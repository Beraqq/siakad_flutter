import 'package:flutter/material.dart';

class DetailKpPage extends StatelessWidget {
  const DetailKpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Detail KP"),),
    );
  }
}